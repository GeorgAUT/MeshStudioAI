# Training

Training is driven by an experiment configuration in `configs/`.

```bash
python src/run_GNN.py --exp_config configs/XXX.yaml
```

A simple starting point is:

```bash
python src/run_GNN.py --exp_config configs/poisson_square_mixed.yaml
```
